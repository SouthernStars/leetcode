# 221.最大正方形
[leetcode 链接](https://leetcode.cn/problems/maximal-square/description/)

### 一、题目描述

在一个由 '0' 和 '1' 组成的二维矩阵内，找到只包含 '1' 的最大正方形，并返回其面积。

### 二、解题思路

动态规划：
// 动态规划
// dp[i][j] 表示i 行 j 列中 以其为左上角的正方形的最大边长，面积即为最长边长的平方
// 状态转移方程 dp[i][j] = matrix[i][j] + min(dp[i+1][j], dp[i][j+1], dp[i+1][j+1])  条件 matrix[i][j]==1
// 初始化 dp[i][j] = matix[i][j] 按照次规则初始化最右列 和最下行


### 三、代码实现

```go
func maximalSquare(matrix [][]byte) int {
    m,n := len(matrix), len(matrix[0])
    dp := make([][]int, m)
    for i:=0; i<m; i++ {
        one := make([]int, n)
        dp[i] = one
    }
    edge_max := 0
    for i:=0; i<n; i++ {
        dp[m-1][i] = int(matrix[m-1][i]-'0')
        edge_max = max(edge_max, dp[m-1][i])
    }
    for i:=0; i<m; i++ {
        dp[i][n-1] = int(matrix[i][n-1]-'0')
        edge_max = max(edge_max, dp[i][n-1])
    }
    
    for i:=m-2; i>=0; i-- {
        for j:=n-2; j>=0; j-- {
            if matrix[i][j] == '1' {
                dp[i][j] = int(matrix[i][j]-'0') + min(dp[i+1][j], min(dp[i][j+1], dp[i+1][j+1]))
                edge_max = max(edge_max, dp[i][j])
            }
        }
    }
    return edge_max * edge_max
}
```
