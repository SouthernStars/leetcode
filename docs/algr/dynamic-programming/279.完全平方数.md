# 279.完全平方数
[leetcode 链接](https://leetcode.cn/problems/perfect-squares/description/)

### 一、题目描述

给你一个整数 n ，返回 和为 n 的完全平方数的最少数量 。

完全平方数 是一个整数，其值等于另一个整数的平方；换句话说，其值等于一个整数自乘的积。例如，1、4、9 和 16 都是完全平方数，而 3 和 11 不是。

### 二、解题思路

完全背包问题

物品 1~sqrt[n]    背包 n   计算凑成 n 的最少平方数数量

dp[i] 表示 凑成 i 的最小平方数数量，dp[0] = 0, 其他初始化成 math.MaxInt32

dp[i] = min(dp[i], dp[i-j*j]+1)  j 1->sqrt(i)

先遍历 背包，后遍历物品

### 三、代码实现

```
func numSquares(n int) int {
    dp := make([]int, n+1)
    dp[0]=0
    for i:=1; i<=n; i++ {
        dp[i] = math.MaxInt32
    }
    for i:=1; i<=n; i++ {
        for j:=1; j<=int(math.Sqrt(float64(i))); j++ {
            dp[i] = min(dp[i], dp[i-j*j]+1)
        }
    }
    return dp[n]
}
```


