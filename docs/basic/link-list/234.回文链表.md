# 234.回文链表
[leetcode 链接](https://leetcode.cn/problems/palindrome-linked-list/description/)

### 一、题目描述

给你一个单链表的头节点 head ，请你判断该链表是否为 回文链表 。如果是，返回 true ；否则，返回 false 。

### 二、解题思路

思路：

### 三、代码实现

```c++
 bool isPalindrome(ListNode* head) {
        if (!head || !head->next) {
            return true;
        }
        ListNode* node = head;
        int len = 0;
        while (node) {
            len++;
            node = node->next;
        }
        node = head;
        for (int i = 1; i < (len + 1) / 2; i++) {
            node = node->next;
        }
        ListNode* p = node->next;
        node->next = nullptr;
        while (p) {
            ListNode* q = p->next;
            p->next = node->next;
            node->next = p;
            p = q;
        }
        node = node->next;
        while (node) {
            if (node->val != head->val) {
                return false;
            }
            node = node->next;
            head = head->next;
        }
        return true;
    }
```

